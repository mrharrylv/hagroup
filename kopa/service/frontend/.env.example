# Kopā has no backend and no secrets. This file exists so the absence is
# deliberate rather than an oversight: if a variable ever appears here, the
# deploy workflow has to start passing it, and the demo stops being static.
